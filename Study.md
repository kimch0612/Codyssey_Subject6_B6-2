# Study Note — B6-2 (AI 커밋/PR 자동 생성 CLI)

과제(subject.txt, README.md)를 진행하기 전에 알아둬야 할 개념과 용어 정리.
보너스 과제(5장)는 진행하지 않고, 기본 요구사항(4장)까지만 완성하는 것을 목표로 함.

## 이 과제의 학습 목적

- AI API를 "그냥 호출"하는 게 아니라, **입력(프롬프트)을 어떻게 설계해야 원하는 품질의 결과가 나오는지** 이해하기 위함
- Git 명령 실행 결과(status/diff)를 프로그램 입력으로 연결하는 자동화 흐름 이해
- REST API 호출 → 응답 처리 → 예외 대응까지 하나의 흐름으로 직접 구현해보는 경험

과제를 마친 뒤 스스로 설명할 수 있어야 하는 것:
- AI API를 REST 방식으로 연동하는 전체 흐름(요청 구성 → 응답 처리 → 예외 대응)
- `temperature`, `max_tokens` 등 주요 파라미터가 결과 품질에 어떤 영향을 주는지
- `git status`, `git diff` 결과를 프로그램 입력으로 연결하는 방식과 자동화 흐름
- Commit/PR 양식과 코드 변경 맥락을 반영해 AI가 요구사항에 맞는 요약을 생성하도록 프롬프트를 구성하는 원리
- 생성된 커밋/PR 텍스트가 실무 규칙(길이·템플릿·표현 제한)을 만족하도록 검증하고 다듬는 이유와 방법

## 최종 산출물

| 산출물 | 최소 규격 |
|---|---|
| CLI 동작 확인 | `commit`/`pr` 명령 실행 시 git 변경사항 수집 → AI API 호출 → 결과가 터미널에 출력, 단일 실행으로 흐름 완주 |
| GitHub 리포지토리 | 소스코드 push, 커밋 히스토리·브랜치 작업 흐름 확인 가능 |
| README.md | 설치법, 환경변수(API Key) 설정법, 실행 예시, 커밋/PR 결과 예시, (민감정보 대응 또는 비용/요청 제한) 1개 이상 포함 |

## 핵심 제약 조건

- AI API Key는 **환경변수로만** 관리, 코드에 하드코딩 금지
- 1회 실행당 AI API 호출은 **1~2회 이내** (commit/pr 각각 1회 권장), 로그에 호출 횟수 출력
- Git 연동 범위는 `git status`/`git diff` 수집까지 — **`git push`, GitHub PR 생성(API) 등 원격 반영 기능은 구현하지 않음**
- diff에 담길 수 있는 민감정보(API Key, 이메일 등)를 다루는 `-safe-mode` 옵션을 최소 1가지 구현 (마스킹 또는 전송량 제한)
- 생성된 커밋/PR 문구는 최종 정답이 아니라 사용자가 검토 후 적용하는 초안
- Python 3.10 이상, 웹 화면 없이 터미널 전용

---

## 용어 정리

실제로 만들게 될 순서(Git 수집 → API 연동 → 프롬프트 설계 → 검증/보안)대로 정리.

### 1. Git 연동

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **`git status`** | 작업 디렉토리의 변경 파일 목록(수정/추가/삭제 등)을 보여주는 명령 | "무엇이 바뀌었는가"의 요약 정보. `--porcelain` 옵션을 쓰면 스크립트가 파싱하기 쉬운 안정적인 출력을 얻을 수 있음 |
| **`git diff`** | 실제 변경된 줄 단위 내용을 unified diff 형식으로 보여주는 명령 | AI에게 "무엇이, 어떻게" 바뀌었는지 알려주는 핵심 입력 텍스트 |
| **Unified diff 포맷** | `+`/`-`로 추가·삭제된 줄을 표시하고 `@@ -a,b +c,d @@`로 위치를 표시하는 표준 diff 표현 | AI 프롬프트에 그대로 넣어도 사람/모델이 이해 가능한 텍스트 포맷이라 별도 파싱 없이 활용 가능 |
| **Staged vs Unstaged diff** | `git diff`는 기본적으로 스테이징 안 된 변경만, `git diff --staged`(`--cached`)는 스테이징된 변경만 보여줌 | 커밋 메시지를 만든다면 보통 "스테이징된" 변경 기준이 자연스러움 — 어떤 걸 수집할지 설계 시점에 결정 필요 |
| **`subprocess` 모듈** | Python에서 외부 명령(git 등)을 실행하고 표준출력/에러를 캡처하는 표준 라이브러리 | `subprocess.run(["git","diff"], capture_output=True, text=True)` 형태로 git 명령 결과를 Python 문자열로 가져오는 데 사용 |
| **현재 브랜치 조회** | `git branch --show-current` 또는 `git rev-parse --abbrev-ref HEAD` | PR 초안에 브랜치명을 표시하기 위해 필요 |

### 2. Python CLI 구성

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **CLI (Command Line Interface)** | 터미널에서 명령어와 옵션으로 프로그램을 조작하는 인터페이스 | 웹 화면 없이 이 과제의 결과물을 사용하는 유일한 방식 |
| **서브커맨드 (subcommand)** | `python main.py commit`, `python main.py pr`처럼 하나의 진입점 아래 여러 동작을 나누는 패턴 | 커밋 생성과 PR 생성이라는 서로 다른 두 기능을 하나의 CLI에서 구분해서 실행하기 위함 |
| **`argparse`** | Python 표준 라이브러리의 CLI 인자/옵션 파싱 모듈 | `-model`, `-temperature`, `-max-tokens`, `-safe-mode` 같은 옵션과 기본값을 정의하는 데 사용. `add_subparsers()`로 서브커맨드 구현 |
| **환경변수 (Environment Variable)** | OS 프로세스에 전달되는 키-값 설정 | Python에서 `os.environ.get("AI_API_KEY")`로 읽음 — API Key를 코드에 하드코딩하지 않기 위한 표준적인 방법 |
| **`.env` / `python-dotenv`** | 로컬 개발 시 환경변수를 파일로 관리하고 자동 로드해주는 도구(선택) | 실행할 때마다 `export`로 키를 넣는 대신 파일로 관리 가능. 단 **`.gitignore`에 반드시 포함**해야 키 유출을 막을 수 있음 |

### 3. AI API / REST API 연동

> **사용 API 확정: Google Gemini API** (2026-08-24) — 무료 티어로 사용 가능해서 선택. 아래 표는 Gemini 기준으로 구체화함.

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **REST API** | HTTP 메서드(GET/POST 등)와 URL, 요청 바디로 서버 기능을 호출하는 방식 | AI API는 보통 POST로 "메시지"를 보내고 "생성된 텍스트"를 JSON으로 응답받는 구조 |
| **Endpoint** | API가 요청을 받는 URL 주소 | Gemini: `https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent` — **모델명이 URL 경로 안에** 들어가는 방식. (참고: OpenAI `.../v1/chat/completions`, Anthropic `.../v1/messages`처럼 제공사마다 endpoint 형태 자체가 다름) |
| **Authorization Header** | 요청에 인증 정보를 담는 부분 | Gemini는 `Authorization: Bearer`가 아니라 **`x-goog-api-key: <KEY>`** 헤더 사용(또는 `?key=<KEY>` 쿼리 파라미터도 가능하지만, URL에 키가 남아 로그·히스토리에 노출될 수 있어 헤더 방식 권장). 값이 없거나 틀리면 401 — 인증 방식도 제공사마다 다르다는 걸 보여주는 사례 |
| **Request Body (JSON)** | 모델명, 메시지, 파라미터 등을 담는 실제 요청 내용 | Gemini는 `messages` 대신 **`contents: [{ role, parts: [{ text }] }]`** 구조 사용. `temperature`/`maxOutputTokens` 같은 파라미터는 메시지와 섞이지 않고 별도의 **`generationConfig`** 객체 안에 들어감 |
| **Response 구조** | 응답 JSON 안에서 생성된 텍스트가 들어있는 위치 | Gemini는 **`candidates[0].content.parts[0].text`** 경로. (참고: OpenAI `choices[0].message.content`, Anthropic `content[0].text`) — 제공사마다 필드명이 달라서 실제 쓰는 API 문서를 보고 정확한 경로로 파싱해야 함 |
| **HTTP 상태 코드** | 200(성공), 401(인증 실패), 403(권한 없음), 429(요청 한도 초과), 500(서버 오류) 등 | "API 호출 실패 시 원인을 포함한 메시지"를 만들려면 상태코드별로 다르게 안내해야 함. Gemini 무료 티어는 RPM/RPD 한도가 낮아서 **429를 실제로 마주치기 쉬움** — 예외 처리 연습에 오히려 도움 |
| **`model` 파라미터** | 사용할 AI 모델 지정 | Gemini는 이 값이 endpoint URL 경로 자체에 들어감(예: `models/gemini-2.5-flash:generateContent`). 정확한 사용 가능 모델명은 자주 바뀌므로 코드 작성 직전 [Google AI Studio](https://ai.google.dev/gemini-api/docs/quickstart)에서 최신 목록 확인 — 여기서 확인한 모델을 `-model` 옵션의 기본값으로 사용 |
| **`temperature`** | 생성 결과의 무작위성(창의성) 조절 값 (Gemini에서는 `generationConfig.temperature`) | 0에 가까울수록 결정적·일관, 높을수록 다양·예측 어려움 — 커밋 메시지처럼 일관성이 중요한 출력엔 낮은 값이 유리 |
| **`max_tokens`** | 응답으로 생성 가능한 최대 토큰 수 (Gemini에서는 **`generationConfig.maxOutputTokens`** — 파라미터 이름이 제공사마다 다른 대표 사례) | 너무 작으면 결과가 중간에 잘리고, 너무 크면 비용/시간이 늘어남. CLI 옵션명(`-max-tokens`)은 그대로 두고 내부에서 `maxOutputTokens`로 매핑하면 됨 |
| **토큰 (Token)** | LLM이 텍스트를 처리하는 최소 단위 | API 비용과 입출력 한도가 토큰 수 기준으로 계산됨 |
| **Context Window** | 모델이 한 번에 참고 가능한 입력+출력 토큰의 최대 범위 | diff가 너무 길면 이 한도를 넘을 수 있음 — safe-mode에서 "diff 일부만 전송"이 필요한 이유 중 하나 |

### 4. 프롬프트 엔지니어링

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **System / User 프롬프트** | AI에게 "역할과 규칙"을 지정하는 시스템 메시지와, 실제 "요청 내용"을 담는 사용자 메시지의 구분 | 커밋/PR 출력 형식 규칙(제목 1줄, 섹션 헤더 등)은 System 프롬프트에 명시하는 게 효과적. **Gemini는 System 프롬프트를 `contents` 배열 안 role이 아니라 별도의 top-level `systemInstruction` 필드로 분리**해서 받음 |
| **Role 기반 메시지 구조** | `{"role": ..., "content"/"parts": ...}` 형태의 메시지 배열로 대화를 표현하는 방식 | 대부분의 AI API가 이 구조를 사용 — 프롬프트를 코드에서 리스트로 조립하게 됨. **Gemini는 role이 `user`/`model` 두 가지뿐**(OpenAI/Anthropic의 `assistant`에 해당하는 이름이 `model`) — system은 role이 아니라 위의 `systemInstruction`으로 별도 처리 |
| **Few-shot Prompting** | 원하는 출력 형식의 예시를 프롬프트에 넣어 AI가 그 패턴을 따라하게 유도하는 기법 | subject.txt의 "결과 예시"를 프롬프트 예시로 활용하면 형식 준수율이 올라감 |
| **구조화된 출력 유도** | "반드시 `## Why`, `## What`, `## How to Test` 섹션으로 답하라"처럼 출력 포맷을 프롬프트에 명시적으로 강제하는 설계 | PR 본문의 템플릿 요구사항(섹션 헤더 + 불릿)을 만족시키는 핵심 방법 |
| **컨텍스트 주입 (Context Injection)** | git diff 텍스트, 변경 파일 목록 같은 실제 데이터를 프롬프트에 끼워 넣는 것 | AI가 "일반적인 답"이 아니라 "이 변경사항에 대한" 답을 하게 만드는 핵심 |

### 5. 예외처리 / 네트워크 오류 대응

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **`try`/`except`** | Python의 예외 처리 구문 | API 호출처럼 실패 가능한 코드를 감싸서, 프로그램이 죽지 않고 에러 메시지를 출력하게 함 |
| **네트워크 예외 종류** | 타임아웃(Timeout), 연결 오류(ConnectionError), 인증 오류(401) 등 | 원인별로 사용자에게 다른 안내가 필요 (예: "API Key 미설정" vs "네트워크 연결 실패") — 뭉뚱그려 "오류 발생"만 출력하면 사용자가 뭘 고쳐야 할지 알 수 없음 |
| **`requests` 라이브러리 / 공식 SDK** | Python에서 HTTP 요청을 보내는 대표 라이브러리, 또는 AI 제공사가 배포하는 공식 클라이언트 | 둘 중 뭘 쓰느냐에 따라 에러가 `requests.exceptions.*`로 오는지, SDK 자체 예외 클래스로 오는지가 달라짐 |

### 6. 보안 / 민감정보

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **정규표현식 (Regex)** | 특정 패턴의 문자열을 찾거나 치환하는 문법 | API Key 형태나 이메일 주소처럼 "패턴이 정해진" 문자열을 diff에서 찾아 마스킹하는 데 사용 |
| **마스킹 (Masking)** | 민감정보를 그대로 노출하지 않고 `sk-****` 처럼 가려서 표시/전송하는 처리 | safe-mode의 (A) 옵션 구현 방식 |
| **`re` 모듈** | Python 표준 라이브러리의 정규표현식 처리 모듈 | `re.sub(pattern, "****", text)` 형태로 마스킹 구현 |
| **Safe Mode 정책 설계** | "AI에게 얼마나 많은 정보를 보낼 것인가"를 정하는 정책 | 패턴 마스킹(A) 또는 전송량 제한 — 예: 최대 파일 수/최대 줄 수(B) 중 최소 1가지 구현 필요 |

### 7. 출력 검증

| 용어 | 뜻 | 왜 알아야 하나 |
|---|---|---|
| **길이 검증** | 문자열 글자 수(`len()`)를 세어 규칙(커밋 제목 72자, PR 제목 80자 등)을 만족하는지 확인 | 요구사항에 명시된 정량적 기준 |
| **섹션 헤더 검증** | PR 본문에 `## Why`, `## What`, `## How to Test`가 실제로 포함돼 있는지 문자열/정규식으로 확인 | 템플릿 구조를 지켰는지 코드로 보장하기 위함 |
| **불릿(bullet) 카운트 검증** | 각 섹션 안에 `-`로 시작하는 줄이 최소 1개 이상 있는지 세는 검증 | "최소 1개 이상의 불릿" 요구사항 체크 |
| **검증 후 재생성 vs 후처리** | 규칙 위반 시 AI에게 다시 요청할지, 코드로 잘라내거나 채워넣을지 | 둘 중 하나(또는 혼합)를 선택해야 하는 설계 지점 — 재생성을 고르면 "API 호출 1~2회 제한"과 충돌하지 않는지 같이 고려해야 함 |

---

## 진행 계획

subject.txt의 기능 요구사항을 학습 순서에 맞게 재배열한 단계별 계획. 구현 세부사항은 일부러 안 적었음 — 직접 설계해보는 게 목적.

### 0단계 — 사전 준비
- **목표**: 개발 환경 세팅, AI API 키 발급, 프로젝트 구조 잡기
- **학습 목표**: ~~어떤 AI API를 쓸지 결정~~ → **Gemini API로 확정 (2026-08-24, 무료 티어)**. 남은 건 API 키 발급 절차, Python 3.10+ 환경 구성
- **완료 조건**: `AI_API_KEY` 환경변수를 설정한 뒤 가장 단순한 요청 하나가 정상 응답을 받는 것까지 확인
- **생각해볼 질문**: API Key가 유출되면 어떤 피해가 생길 수 있고, 막기 위한 최소한의 습관은 뭘까?

### 1단계 — Git 변경사항 수집 모듈
- **목표**: `git status`, `git diff` 결과를 Python에서 문자열로 가져오는 함수 작성
- **학습 목표**: `subprocess` 사용법, unified diff 포맷 읽는 법, staged/unstaged 차이
- **완료 조건**: 변경사항이 있을 때/없을 때 각각 올바르게 동작(없으면 안내 메시지 후 종료)
- **생각해볼 질문**: `git diff`만 보내는 것과 `git status` 요약까지 같이 보내는 것, AI 결과 품질에 어떤 차이가 있을까?

### 2단계 — AI API 호출 기본 골격
- **목표**: 환경변수에서 API Key를 읽고, 최소한의 요청 하나를 보내 응답을 받는 함수 작성
- **학습 목표**: REST 요청 구조(헤더/바디), 응답 JSON에서 텍스트 추출하는 법, 상태코드별 예외 처리
- **완료 조건**: API Key 미설정 시 안내 메시지, 네트워크/인증 실패 시 원인이 드러나는 에러 메시지 출력
- **생각해볼 질문**: 이 실패 메시지들을 사용자가 "뭘 해야 할지 바로 알 수 있게" 쓰려면 어떻게 써야 할까?

### 3단계 — 프롬프트 설계 + `commit` 명령
- **목표**: git diff/status를 컨텍스트로 넣어 커밋 메시지를 생성하는 프롬프트 설계, `commit` 서브커맨드 구현
- **학습 목표**: System/User 프롬프트 역할 분리, few-shot 예시로 출력 형식 유도, temperature를 낮게 두는 이유
- **완료 조건**: 제목 1줄 + (선택) 본문 출력, 본문 포함 시 파일/변경사항 언급 최소 1개
- **생각해볼 질문**: 같은 diff로 temperature를 0과 1로 각각 돌려보면 결과가 실제로 어떻게 달라지는가?

### 4단계 — `pr` 명령 (제목/본문 템플릿)
- **목표**: Why/What/How to Test 구조를 강제하는 프롬프트로 PR 제목/본문 생성
- **학습 목표**: 구조화된 출력을 프롬프트로 강제하는 기법, 브랜치명 등 추가 컨텍스트 활용
- **완료 조건**: 3개 섹션 헤더 + 섹션당 불릿 최소 1개, 제목 80자 이내
- **생각해볼 질문**: AI가 섹션 헤더나 형식을 빼먹는 경우(프롬프트만으론 100% 강제가 안 됨)를 코드로 어떻게 보완할 것인가?

### 5단계 — 출력 검증/후처리
- **목표**: 생성된 텍스트가 길이/형식 규칙을 만족하는지 검사하고, 어길 경우 재생성하거나 다듬는 로직 추가
- **학습 목표**: 검증 로직 설계(정규식/문자열 검사), "재생성 vs 후처리"의 트레이드오프
- **완료 조건**: 규칙 위반 케이스를 일부러 만들어봐도 최종 출력은 규칙을 만족
- **생각해볼 질문**: 재생성을 선택하면 "API 호출 1~2회 제한"과 충돌하지 않는가? 이 제약을 어떻게 지킬 것인가?

### 6단계 — Safe Mode 구현
- **목표**: diff에 담긴 민감정보를 마스킹하거나 전송량을 제한하는 `-safe-mode` 옵션 구현
- **학습 목표**: 정규식으로 API Key/이메일 패턴 찾기, "얼마나 가릴지"에 대한 정책 설계
- **완료 조건**: safe-mode를 켰을 때/껐을 때 실제로 AI에게 전송되는 내용이 달라짐을 확인
- **생각해볼 질문**: 마스킹이 너무 과하면 AI가 맥락을 이해 못 하게 될 수도 있는데, 그 균형을 어떻게 잡을까?

### 7단계 — CLI 옵션화
- **목표**: `argparse`로 `-model`, `-temperature`, `-max-tokens` 등 옵션과 기본값 정의
- **학습 목표**: 서브커맨드(`commit`/`pr`)별 공통 옵션을 어떻게 구성할지
- **완료 조건**: 옵션 없이 실행하면 기본값으로 동작, 옵션을 주면 그 값으로 API 호출이 바뀜
- **생각해볼 질문**: 기본값을 정할 때 "결과 품질"과 "비용/속도" 중 뭘 우선했는가?

### 8단계 — 문서화 + GitHub Push
- **목표**: README.md 작성, 리포지토리 push
- **학습 목표**: 실행 방법을 "처음 보는 사람도 그대로 따라할 수 있게" 쓰는 법
- **완료 조건**: 설치법 / 환경변수 설정법 / 실행 예시 / 출력 예시 / (민감정보 대응 또는 비용·요청 제한 안내) 포함
- **생각해볼 질문**: 이 README만 보고 다른 사람이 그대로 실행할 수 있는가?

---

## 시연 시뮬레이션 (구현 전 리허설)

코드를 짜기 전에, subject.txt 요구사항을 기준으로 "완성되면 시연이 어떻게 흘러갈지"를 미리 시뮬레이션. 각 단계가 실제 구현의 완료 기준이 되기도 함. (보너스 관련 시연은 포함하지 않음)

### 사전 준비
- `.env` 대신 **터미널에서 직접 `export AI_API_KEY="발급받은_키"`** 로 등록하기로 결정 — 시연 직전 `echo $AI_API_KEY`로 값이 비어있지 않은지 먼저 확인
- 시연 대상 프로젝트: 기본은 **B6-2 자기 자신**(실제 작업 변경사항 활용). 통제된 예시(safe-mode 등)가 필요할 때만 별도 더미 repo를 만들고, `python3 <B6-2 절대경로>/main.py <명령>` 형태로 **경로만 가리켜서** 실행 (복사 금지 — 이전 대화에서 정리한 이유: 버전 불일치 방지)
- 시연 시작 전 `git status`로 지금 어떤 변경사항이 잡히는지 먼저 확인해서, 의도와 다른 지저분한 WIP 상태면 정리

### 1. 정상 흐름 — 커밋 메시지 생성
```
python main.py commit
```
기대 출력:
```
[INFO] Git status 수집 완료: N개 파일 변경 감지
[INFO] Git diff 수집 완료: N줄
[INFO] AI API 요청 중... (호출 1회)
[DONE] 커밋 메시지 생성 완료

--- Commit Message ---
<커밋 제목 1줄>

- <변경 파일/모듈 언급 또는 핵심 변경 불릿>
----------------------
```
체크리스트: [ ] 제목 1줄(50자 권장/72자 이내) · [ ] 본문 포함 시 파일 언급 또는 불릿 최소 1개 · [ ] API 호출 횟수 로그로 확인 가능 · [ ] 구분선으로 결과가 구획됨

### 2. 정상 흐름 — PR 제목/본문 생성
```
python main.py pr
```
기대 출력:
```
[INFO] 현재 브랜치: <브랜치명>
[INFO] AI API 요청 중... (호출 1회)
[DONE] PR 초안 생성 완료

--- PR Title ---
<PR 제목 1줄>

--- PR Body ---
## Why
- ...
## What
- ...
## How to Test
- ...
```
체크리스트: [ ] 제목 80자 이내 · [ ] Why/What/How to Test 3개 섹션 헤더 모두 존재 · [ ] 섹션마다 불릿 최소 1개 · [ ] 브랜치명 표시

### 3. 예외 상황 — API Key 미설정
```
unset AI_API_KEY
python main.py commit
```
기대 출력:
```
[ERROR] AI_API_KEY 환경변수가 설정되지 않았습니다.
## 예) export AI_API_KEY="YOUR_KEY"
```
체크리스트: [ ] 프로그램이 죽지 않고 안내 후 정상 종료 · [ ] 해결 방법(export 예시)까지 안내
> 시연 후 반드시 `export AI_API_KEY=...`로 복구할 것

### 4. 예외 상황 — 변경 사항 없음
클린 상태(또는 커밋 직후)에서:
```
python main.py commit
```
기대 출력:
```
[INFO] 변경 사항이 없습니다. 커밋 메시지를 생성하지 않고 종료합니다.
```
체크리스트: [ ] AI API를 호출하지 않고 즉시 종료 (불필요한 과금/호출 방지 확인용)

### 5. CLI 옵션 커스터마이징
```
python main.py commit -model gemini-3.1-flash-lite -temperature 0.2 -max-tokens 300
```
체크리스트: [ ] 옵션 값이 실제 요청에 반영됨(로그로 확인 가능해야 함) · [ ] 옵션 생략 시 기본값으로 정상 동작

### 6. Safe Mode 비교
준비: 별도 더미 repo에 가짜 API 키/이메일 형태 문자열이 포함된 변경을 만들어둠
```
python main.py commit                # safe-mode 없이
python main.py commit -safe-mode     # safe-mode 적용
```
체크리스트: [ ] safe-mode ON일 때 민감정보 패턴이 마스킹되거나 전송 범위가 제한됨 · [ ] OFF일 때와 나란히 비교해서 차이를 보여줌

### 7. GitHub 리포지토리 확인
브라우저로 GitHub repo 페이지 오픈. 체크리스트: [ ] 소스 코드/파일 구조 정상 업로드 · [ ] 커밋 히스토리에 작업 흐름이 드러남 · [ ] 브랜치 작업 흐름 확인 가능

### 8. README.md 확인
문서만 보고 실행 가능해야 함. 체크리스트: [ ] 설치 및 실행 방법 · [ ] 환경변수(API Key) 설정 방법 · [ ] 커밋/PR 생성 명령 사용 예시 · [ ] 출력 예시(커밋 메시지/PR 본문) · [ ] 민감정보 대응 또는 비용·요청 제한 안내 중 최소 1개

### 최종 제출 체크리스트 (subject.txt 4장 총정리)
- [ ] git status/diff 수집, 변경 없으면 안내 후 종료
- [ ] API Key 환경변수 관리 (하드코딩 없음)
- [ ] API 호출 실패 시 원인 포함 에러 메시지
- [ ] model/temperature/max-tokens CLI 옵션화 + 기본값
- [ ] commit 명령: 제목 1줄 필수 (+ 선택 본문 규칙)
- [ ] pr 명령: Why/What/How to Test 섹션 + 섹션별 불릿
- [ ] 길이 규칙 (커밋 제목 72자, PR 제목 80자) 검증/후처리
- [ ] 구분선/헤더로 출력 구획
- [ ] safe-mode 옵션 최소 1가지 구현
- [ ] GitHub push 완료
- [ ] README.md 필수 항목 전부 포함
